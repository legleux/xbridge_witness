name: "Build xbridge_witnessd"
description: ""
runs:
  using: "composite"
  steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build
      shell: bash
      run: . ./.github/actions/build/build_xbwd.sh
